__pycache__/
*.pyc
.venv/
venv/

# Secrets -- never commit these
.env
config/credentials.json
config/token.json

# Mutable runtime config edited via the control panel -- auto-created
# with defaults by config/rules.py on first use of either app.
config/rules.json

# Runtime output
logs/
output/
config/.schedule_state.json
